# 公开发布前检查

检查日期：2026-09-08。结论：**暂不直接公开当前完整 Git 历史**。

## 已授权的隐私处理

公开 PRD 已将真实候选人背景替换为明确标注的虚构统计学本科画像；年份场景只用于资格测试，不能用于推断作者真实经历。新建 `public-release` 发布分支，不继承私人开发历史。原 `main` 保留完整本地历史，不删除或重写。

发布提交使用中性项目身份（保留域名 `.invalid` 的占位邮箱），不是作者真实邮箱，也不是经过 GitHub 验证的账号身份。没有更改全局 Git 配置。依赖告警尚未处理，此分支不等于已获准上线。

只检查发布分支：`python scripts/audit_public_history.py --ref public-release`。默认不传参数仍会扫描包括本地 `main` 在内的所有引用，因此仍可能报告私人历史。扫描无命中也不构成完整安全证明。

将来推送前必须核实远端和明确的分支映射，只推送经过核验的发布分支。**不要使用 `--all`、`--mirror`、`--tags` 或把 main 合并到发布分支**，否则可能带入保留的旧历史。当前没有执行任何推送。

以下为处理前审计记录，保留用于说明决策依据。

## 已执行范围

- 扫描检查开始时全部本地引用可达的 21 个提交、102 个唯一文件对象，包括 XLSX 中的 XML。未读取钥匙串或被忽略的真实候选人目录。
- 常见密钥、手机号、邮箱、本地路径模式筛查；扫描只报告文件和类别，不输出匹配值。
- 检查历史文件路径和提交作者元数据。二进制 PNG 不由文本扫描判定安全；两张公开演示图此前已人工视觉核验为虚构数据。
- 使用临时隔离环境的 pip-audit 2.10.1 查询项目虚拟环境已安装包。仅发送公开包名称及版本，不发送项目源码或个人资料。
- 72 项工程测试通过，不等于安全审计全部通过。

## 发布阻碍

| 范围 | 发现 | 建议处理 |
| --- | --- | --- |
| Git 历史 | 旧版 README 含本机用户路径，旧版表格导出脚本含真实姓名 | 不能仅删除当前文件；明确选择脱敏历史或无旧历史的发布分支 |
| 当前 PRD | 仍有真实毕业时间、专业、实习时长等候选人背景 | 由作者确认哪些信息愿意公开，或替换为明确标注的示例画像 |
| 提交元数据 | 一个作者身份，邮箱不是 GitHub noreply 格式 | 确认公开意愿；否则发布提交使用经过确认的公开身份 |
| Python 依赖 | pypdf 5.9.0、pytest 8.4.2、pip 25.2 命中已知漏洞数据库 | 升级兼容性测试、复审依赖上限后重新扫描 |

当前及历史未发现被跟踪的真实 DOCX/PDF 简历、SQLite 数据库或私有目录路径下的文件；模式扫描未命中常见 API Key / 私钥内容。此结论不是“无任何敏感信息或漏洞”的保证。

## 依赖扫描解释

工具共返回 49 条漏洞记录：pypdf 41、pip 7、pytest 1。pip 中存在重复 ID，因此不将 49 称为去重后的漏洞数。漏洞命中不证明本应用存在可利用路径，仍需逐项确认影响。项目自身未发布到 PyPI，工具跳过项目包，不能据此评价自有代码安全。

返回的修复版本包含 pypdf 6.16.1、pytest 9.0.3、pip 26.2；这些是本次数据库返回值，尚未完成升级验证，不作为兼容性保证。项目目前限制 `pypdf<6`、`pytest<9`，需要有意识地调整约束。pip 属于开发/安装环境工具，不是项目运行时依赖。

本轮未修改这些依赖、未重写提交历史、未改变作者身份，也未向 GitHub 上传任何内容。原始漏洞报告保存在本地临时审计目录，不提交包含完整环境清单的原始输出。

## 后续安全操作

先确认个人信息的公开范围及历史处理方式。保留本地完整历史的发布分支方案会产生新提交，不能自动防止误推旧分支或标签，因此真正推送前仍须逐引用检查。未经授权不执行历史重写或强制推送。

之后单独升级依赖、重跑全部测试与漏洞扫描，完成 GitHub 首次 CI 验证后再决定公开。Python 包版本范围仍未锁定，当前安装环境的审计不代表未来安装结果。

启发式历史扫描可复现：`python scripts/audit_public_history.py`。范围为 `git rev-list --all` 可达对象，不包含 reflog/不可达对象或完整语义审查；输出路径本身也可能敏感，不应未经检查直接公开。该脚本不是成熟的秘密扫描器，也不替代人工检查。
